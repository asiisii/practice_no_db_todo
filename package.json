{
	"name": "react-ts-tailwind-rtk-query",
	"scripts": {
		"start": "concurrently \"nodemon server/server.ts\" \"cd client && yarn run dev\"",
		"build": "cd client && yarn run build"
	},
	"devDependencies": {
		"concurrently": "^7.6.0"
	}
}
